name: PHPCS check

on: pull_request

permissions:
  actions: read
  checks: read
  contents: read
  deployments: none
  issues: read
  packages: none
  pull-requests: read
  repository-projects: none
  security-events: none
  statuses: read

jobs:
  phpcs:
    name: PHPCS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          lfs: false
          persist-credentials: false
          php-version: '7.2'
          tools: cs2pr
      - name: Check PHP code styles
        id: phpcs
        run: ls -l;./vendor/bin/phpcs --report-full --standard=plugins/GoogleAnalyticsImporter/phpcs.xml --report-checkstyle=./phpcs-report.xml plugins/GoogleAnalyticsImporter
      - name: Show PHPCS results in PR
        if: ${{ always() && steps.phpcs.outcome == 'failure' }}
        run: cs2pr ./phpcs-report.xml --prepend-filename